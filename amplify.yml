version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:static
  artifacts:
    baseDirectory: .next # Statik ve dinamiğin birleştiği yer
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
