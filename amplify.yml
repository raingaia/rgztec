version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:static
  artifacts:
    baseDirectory: docs # Kritik ayar burası!
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
