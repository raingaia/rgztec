version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Docs only build"
    build:
      commands:
        - echo "No build step (static docs)"
  artifacts:
    baseDirectory: docs
    files:
      - "**/*"
  cache:
    paths: []

