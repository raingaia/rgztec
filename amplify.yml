version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci || npm install
    build:
      commands:
        - node scripts/copy-docs-data.mjs
  artifacts:
    baseDirectory: docs
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*



