version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:static  # Kendi özel scriptini çalıştır
        - npx next build        # Next.js'i zorla derle
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
